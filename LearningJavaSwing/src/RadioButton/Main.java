package RadioButton;

public class Main {
	public static void main(String[] args) {
		/*
		 * Radio Button = one or more button in a grouping in which only 1 maybe selected per group
		 */
		new MyFrame();
	}
}
